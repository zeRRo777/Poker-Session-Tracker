

# ♠️ Poker Session Tracker

Простое и мощное приложение для учета результатов покерных сессий, анализа винрейта и ведения банкролл-менеджмента. Написано на Python с использованием Streamlit.

![Streamlit](https://img.shields.io/badge/Streamlit-FF4B4B?style=for-the-badge&logo=Streamlit&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.9%2B-blue?style=for-the-badge&logo=python&logoColor=white)
![SQLite](https://img.shields.io/badge/SQLite-07405E?style=for-the-badge&logo=sqlite&logoColor=white)

## ✨ Возможности

*   **Дашборд:** Общая прибыль, почасовой доход (Hourly Rate), ROI, Win Rate и графики профита.
*   **Журнал сессий:** Удобное добавление, редактирование (прямо в таблице) и удаление сессий.
*   **Гибкие фильтры:** Фильтрация статистики по датам, покер-румам и типам игр (Cash, MTT, Spin&Go).
*   **Справочники:** Управление списком румов и дисциплин.
*   **Локальное хранение:** Все данные хранятся в `poker_stats.db` на вашем компьютере. Никаких облаков.

### **ROI считается только если у вас добавлен тип игры "MTT" и считается только для сессий данного типа.**

## 🚀 Установка и запуск

### Предварительные требования
У вас должен быть установлен [Python](https://www.python.org/downloads/) (версия 3.8 или выше).

### 1. Клонирование репозитория
```bash
git clone https://github.com/ВАШ_НИКНЕЙМ/poker-tracker.git
cd poker-tracker
```

### 2. Создание виртуального окружения (рекомендуется)
Windows:
```bash
python -m venv venv
venv\Scripts\activate
```
macOS/Linux:
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Запуск приложения
```bash
streamlit run app.py
```
После ввода команды приложение автоматически откроется в вашем браузере по адресу `http://localhost:8501`.

## 📂 Структура проекта

*   `app.py` — Основной файл интерфейса (Streamlit).
*   `database.py` — Работа с SQLite (создание таблиц, CRUD операции).
*   `logic.py` — Логика вычислений (KPI, стрики, форматирование).
*   `poker_stats.db` — База данных (создается автоматически при первом запуске).

## 🛠 Технологический стек

*   **Frontend:** Streamlit
*   **Backend:** Python
*   **Database:** SQLite
*   **Analytics:** Pandas, Plotly Express

### Создание Ярлыка на рабочем столе

1. Нажмите правой кнопкой мыши на файл run_tracker.bat -> Отправить -> Рабочий стол (создать ярлык).
2. На рабочем столе появится ярлык. Нажмите на него правой кнопкой -> Свойства.
3. Сменить значок (Change Icon): Вы можете скачать иконку в формате .ico и выбрать её. В корне приложения есть иконка card_icon.ico, если вас устраивает, то вы можете использовать ее.
4. В поле "Окно" (Run) выберите Свернутое в значок (Minimized). Тогда черное окно консоли не будет висеть посреди экрана, а свернется вниз.
